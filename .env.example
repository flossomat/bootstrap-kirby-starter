# FTP Deployment Konfiguration
FTP_HOST=ftp.example.com
FTP_USER=username
FTP_PASSWORD=password

# Optional: SFTP Port (Standard: 22)
FTP_PORT=22

# Optional: Remote Pfad
REMOTE_PATH=/html

# Optional: Local Development URL (wird automatisch aus Projektordner generiert wenn leer)
LOCAL_URL=
